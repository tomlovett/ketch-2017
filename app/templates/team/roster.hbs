<div class="layout flex-row">
  {{player-list
      players=men
      playerClass=team.primary
      class='flex-30'
  }}

  {{player-list
      players=women
      playerClass=team.secondary
      class='flex-30'
  }}
  {{!-- add  player --}}
</div>
